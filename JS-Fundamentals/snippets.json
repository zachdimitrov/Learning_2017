{
    /*
    	 // Place your snippets for JavaScript here. Each snippet is defined under a snippet name and has a prefix, body and 
    	 // description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
    	 // $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
    	 // same ids are connected.
    	 // Example:
    	 "Print to console": {
    		"prefix": "log",
    		"body": [
    			"console.log('$1');",
    			"$2"
    		],
    		"description": "Log output to console"
    	}
    */
    "Print to console": {
        "prefix": "con",
        "body": [
            "console.log('$1');",
            "$2"
        ],
        "description": "Log output to console"
    },
    "for loop with length": {
        "prefix": "for_array",
        "body": [
            "for (let ${1:i} = 0, len = ${2:arr}.length; ${1:i} < len; ${1:i} += 1) {$0}"
        ],
        "description": "Loop through array"
    },
    "for loop with length backward": {
        "prefix": "for_reverse",
        "body": [
            "for (let ${1:i} = ${2:arr}.length - 1; ${1:i} >= 0; ${1:i} -= 1) {$0}"
        ],
        "description": "Loop through array backwards"
    },
    "function solve (args) {}": {
        "prefix": "f_args",
        "body": [
            "function solve(args) {",
            "	${1:let n = +args[0],}$0",
            "}"
        ],
        "description": "Function declaration"
    },
    "multiline cooment": {
        "prefix": "com",
        "body": [
            "/*",
            "$0",
            "*/"
        ],
        "description": "Multi Line Comment"
    },
	"reverse elements": {
		"prefix": "reverse",
		"body": [
			"if (${1:a} < ${2:b}) {",
			"	let k = ${2:b};",
			"	${2:b} = ${1:а};",
			"	${1:а} = к;",
			"}",
			"$0"
		],
		"description": "Reverse two compareable variables"
	}
}